{
  "scripts": {
    "dev": "hugo mod tidy && hugo server --logLevel debug --disableFastRender -p 1313",
    "probe:yt": "go run ./scripts/ci/probe.go",
    "update:yt": "node scripts/ci/youtubei.mjs",
    "build": "npm run build:prod",
    "build:prod": "hugo mod tidy && npm run update:yt && HUGO_ENVIRONMENT=production HUGO_ENV=production sh -c 'hugo --gc --minify ${BASE_URL:+--baseURL \"$BASE_URL\"}'",
    "build:prod:mr-preview": "BASE_URL=\"${PREVIEW_URL:?Missing PREVIEW_URL}\" npm run build:prod"
  },
  "dependencies": {
    "tailwindcss": "^4.1.18",
    "youtubei.js": "^16.0.1",
    "@tailwindcss/cli": "^4.1.18",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6"
  }
}
